# Test-only dependencies (no torch / no GPU required).
#   pip install -r requirements-dev.txt
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
httpx==0.27.2
pytest==8.3.3
# Used by test_prompt_contract to render the tokenizer chat template
# exactly as the serving backend does.
jinja2==3.1.6
